# Cloudflare Workers - static asset hosting for the INGOT site.
# Serves the contents of ./site as a static website (no server code).
name = "velke-hamry"
compatibility_date = "2026-06-15"

[assets]
directory = "./site"
